# Archived project material

`legacy_root_flutter_project/` is the earlier, incomplete Flutter project that
previously occupied the repository root. The active Barangay Management System
is now the single Flutter project at the repository root.

`generated_flutter_artifacts/` is intentionally ignored and contains only
local build/cache material retained during the consolidation. It is not needed
to run the app.
