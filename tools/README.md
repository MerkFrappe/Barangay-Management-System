# Local utility material

`scratch_automate/` contains legacy Firebase/browser automation experiments.
It is intentionally ignored because it includes generated browser state and
credential-adjacent helper material. It is not part of the Flutter app or its
commit.
